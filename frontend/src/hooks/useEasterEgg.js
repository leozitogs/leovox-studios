/**
 * useEasterEgg — Stub hook (placeholder)
 */
import { useState } from 'react';

const useEasterEgg = () => {
  const [isActive, setIsActive] = useState(false);
  return { isActive, setIsActive };
};

export default useEasterEgg;
